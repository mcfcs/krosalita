# Supabase — needed for Multiplayer (Phase 2) and Profiles/Saved puzzles (Phase 3).
# Leave blank to run the app without accounts/multiplayer (Browse + local play still work).
# Create a project at https://supabase.com, then copy the Project URL + anon key.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
